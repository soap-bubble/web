FROM node:dubnium
RUN mkdir -p /opt/app
COPY . /opt/app
WORKDIR /opt/app
RUN npm install --quiet;\
    npm run build:staging;\
    npm install --production --quiet
ENV NODE_ENV=production
CMD node lib/index
EXPOSE 80
