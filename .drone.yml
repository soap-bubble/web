pipeline:
  build:
    image: node:boron
    commands:
      - npm install
    when:
      branch: master
      event: push

  publish:
    image: plugins/docker
    repo: docker.soapbubble.online:5000/soapbubble/online
    tag: latest
    when:
      branch: master
      event: push
