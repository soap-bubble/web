// import React, { FunctionComponent, useEffect, useState, useMemo } from 'react'
// import TwitchEmoji from './TwitchEmoji'

// const elastic = (n: number) =>
//   n * (33 * n * n * n * n - 106 * n * n * n + 126 * n * n - 67 * n + 15)
// const inExpo = (n: number) => Math.pow(2, 10 * (n - 1))

// const Bounce: FunctionComponent<{
//   id: number
//   x: number
//   y: number
//   scale: [number, number, number]
//   opacity: number
// }> = ({ id, x, y, scale, opacity }) => {
//   return <TwitchEmoji id={id} alpha={opacity} pos={[x, y, -10]} scale={scale} />
// }

// export default Bounce
