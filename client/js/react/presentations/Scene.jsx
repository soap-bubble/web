import React, { Component } from 'react';
import Canvas from './canvas';

export default class extends Component {
  componentDidMount() {
    if (this.props.data) {
      
    }
  }

  render() {
    return (
      <Canvas />
    );
  }
}
