pipeline:
  install:
    image: node:boron
    commands:
      - npm install
      - npm run bootstrap
    when:
      event: [ push, pull_request ]


  test:
    image: node:boron
    commands:
      - npm test
    when:
      event: [ push, pull_request ]
